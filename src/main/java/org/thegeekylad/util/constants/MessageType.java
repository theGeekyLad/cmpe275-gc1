package org.thegeekylad.util.constants;

public enum MessageType {
    ETL, HBT, ELC, QRY, NEW, RNG;

//    public static MessageType getType(String name) {
//        if (name.equals(ETL.name())) return ETL;
//        if (name.equals(HBT.name())) return HBT;
//        if (name.equals(ELC.name())) return ELC;
//        if (name.equals(QRY.name())) return QRY;
//        if (name.equals(NEW.name())) return NEW;
//        if (name.equals(RNG.name())) return RNG;
//
//        return null;
//    }
}
